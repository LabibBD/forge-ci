name: alternating-policy-candidate
seed: 42
episodes: 10

environment:
  name: line_world
  goal: 5
  max_steps: 10
  slip_probability: 0.0

policy:
  name: alternating

gate:
  min_success_rate: 1.0
  max_success_rate_drop: 0.0
  max_mean_steps_increase: 1.0
